package uiMain;

import java.util.Scanner;

import gestorAplicacion.*;

/*public class Main {
  public static void main(String[] args) {
    Usuario usuario1 = new Usuario("carlos", 0, 0, null, 0);
    Empleado empleado1 = new Empleado("luis pedro", 0, 0, null, 0, 0);
    Habitacion hab1 = new Habitacion(0, 0, null, 0);
    Servicio ser1 = new Servicio("piscina", 34);

    Factura f1 = new Factura(usuario1, empleado1, new Servicio[] { hab1, ser1 });

    // Suponiendo que ya se genero la reserva y factura
    System.out.println(f1.PrintFactura());

    Scanner sc=new Scanner(System.in);

    System.out.println("¿Desea realizar el pago de la factura de imenmediato?");

    int opt=sc.nextInt();

    switch (opt) {
      case 1:
          // código a ejecutar si la variable es igual a valor1
          break;
      case 2:
          // código a ejecutar si la variable es igual a valor2
          break;
      // puedes añadir más casos si lo necesitas
      default:
          System.out.println( "Respuesta Invalido");
          break;
    }

    sc.close();

  }

}
*/